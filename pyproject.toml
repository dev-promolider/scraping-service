[project]
name = "scrapegraph-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "joansolignac", email = "joanpsolignac@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "python-dotenv>=1.2.2",
    "scrapegraphai>=2.1.6",
    "uvicorn[standard]>=0.52.1",
]

[project.scripts]
extraer-cursos = "scrapegraph_api.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
